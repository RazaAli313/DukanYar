[project]
name = "dukanyar-backend"
version = "0.1.0"
description = "DukanYar backend — FastAPI service for the text & voice conversation module"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic-settings>=2.15.0",
]
